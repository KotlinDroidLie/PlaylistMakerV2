<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settings"
    android:background="@color/white"
    android:orientation="vertical">

 <com.google.android.material.appbar.MaterialToolbar style="@style/ToolBarStyle"
     android:id="@+id/button_back"
     app:title="@string/text_settings"
     app:titleTextColor="@color/black"
     app:navigationIcon="@drawable/arrow_back"
     android:paddingBottom="24dp"/>

    <com.google.android.material.switchmaterial.SwitchMaterial style="@style/TextViewStyleSettings"
        android:text="@string/themes_dark"/>

    <com.google.android.material.textview.MaterialTextView style="@style/TextViewStyleSettings"
        android:drawableEnd="@drawable/share"
        android:text="@string/share_app"/>

    <com.google.android.material.textview.MaterialTextView style="@style/TextViewStyleSettings"
        android:drawableEnd="@drawable/support"
        android:text="@string/write_to_support"/>

    <com.google.android.material.textview.MaterialTextView style="@style/TextViewStyleSettings"
        android:drawableEnd="@drawable/arrow_forward"
        android:text="@string/user_agreement"/>

    </LinearLayout>